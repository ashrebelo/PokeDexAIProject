<header>
  <a href="/">Home</a>
  <% if (user) { %>
    <p>Signed in as <%= user.displayName || user.email || 'Google user' %></p>
    <a href="/favorites">Favorites</a>
    <a href="/auth/logout">Logout</a>
  <% } else { %>
    <a href="/auth/google">Sign in with Google</a>
  <% } %>
</header>